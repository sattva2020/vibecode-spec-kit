import { ProjectInfo } from "./ProjectTypes";export interface RFCDocument {id: string;title: string;abstract: string;status: "draft" | "active" | "deprecated";sections: RFCSection[];references: string[];lastUpdated: Date;}export interface RFCSection {id: string;title: string;content: string;type: "text" | "code" | "diagram" | "table";}export interface RFCBuilder {buildArchitectureDecisions(project: ProjectInfo): RFCDocument;buildTechnicalStandards(project: ProjectInfo): RFCDocument;buildSecurityGuidelines(project: ProjectInfo): RFCDocument;generateMarkdown(rfc: RFCDocument): string;}
